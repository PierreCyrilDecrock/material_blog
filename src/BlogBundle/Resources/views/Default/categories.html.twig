{% extends 'BlogBundle::layout.html.twig' %}

{% block body %}
  {% for category in categories %}
    <a href="{{ path ('bundle_categories') }}">
      <h4>{{ category.name}}</h4>
    </a>

  {% endfor %}
{% endblock %}
